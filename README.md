# XR Experiments at xr.webvr.dev

This repo contains experiments in WebXR related technologies.

## Usage

The `build/` is where any generated content should go, though the `build/ex` directory
should not be overwritten, it contains basic examples that are hand written.
